'use babel';

import PluginVaultOneLineView from '../lib/plugin-vault-one-line-view';

describe('PluginVaultOneLineView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
